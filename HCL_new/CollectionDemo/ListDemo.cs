using System;
using System.Collections.Generic;
using System.Text;
using System.Collections;
using System.Linq;

namespace HCL_new.CollectionDemo
{
    class ListDemo
    {
        static void Main(string[] args)
        {
            /*  List<Student> lst = new List<Student>();
             // lst.Add(new Student(1, "Pradnya", 90));
              do
              {
                  Console.WriteLine("Enter id,name,percentage");
                  int id = int.Parse(Console.ReadLine());
                  string name = Console.ReadLine();
                  int per = int.Parse(Console.ReadLine());
                  Student s = new Student(name, id, per);
                  lst.Add(s);
                  Console.WriteLine("Do u want to add more(Y/N)");
                  char ch = char.Parse(Console.ReadLine());

                  if (ch != 'Y')
                  {
                      break;
                  }
              }
              while (true);
              foreach (Student s1  in lst)
              {
                  Console.WriteLine(s1);
              }

          }*/
        }
    }
}
